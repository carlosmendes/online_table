<%= form_for(@sub_category, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @sub_category.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@sub_category.errors.count, "error") %> prohibited this sub_category from being saved:</h4>

      <ul>
      <% @sub_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :category_id, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.select :category_id, Category.all.map { |p| [p.name, p.id] }, {:include_blank => '-' }, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :order, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.number_field :order, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :active, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.check_box :active, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
